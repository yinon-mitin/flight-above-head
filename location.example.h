#pragma once

// Copy this file to location.h and enter the fixed installation
// coordinates. location.h is intentionally ignored by Git because exact home
// coordinates are private even though the Open-Meteo API itself is keyless.

#define HOME_LATITUDE 00.000000
#define HOME_LONGITUDE 00.000000
